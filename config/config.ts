/**************************************************************************************************
*
*            Application configuration, e.g. server ports, db names
*
*/

export const config = {
    port: {
        server: 8080
    },
};

console.log('ok');